---
- name: Verify cloud image syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.rhel.cloud_image coverage stub ran
      ansible.builtin.assert:
        that:
          - cloud_image_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
