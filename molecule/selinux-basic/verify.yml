---
- name: Verify selinux syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.rhel.selinux coverage stub ran
      ansible.builtin.assert:
        that:
          - selinux_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
