---
- name: Verify vscode syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Assert lit.rhel.vscode coverage stub ran
      ansible.builtin.assert:
        that:
          - vscode_molecule_coverage_mode | default('') == 'syntax_stub'
        quiet: true
