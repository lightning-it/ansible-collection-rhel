---
- name: Converge vscode config syntax coverage
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    vscode_config_molecule_execute_role: false
  tasks:
    - name: Import lit.rhel.vscode_config for syntax coverage
      ansible.builtin.import_role:
        name: lit.rhel.vscode_config
      when: vscode_config_molecule_execute_role | bool

    - name: Record lit.rhel.vscode_config Molecule coverage mode
      ansible.builtin.set_fact:
        vscode_config_molecule_coverage_mode: syntax_stub
