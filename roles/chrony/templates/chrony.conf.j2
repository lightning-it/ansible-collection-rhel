# Generated by lit.rhel.chrony
driftfile {{ chrony_driftfile }}
makestep {{ chrony_makestep }}

{% for pool in chrony_pools | default([]) %}
pool {{ pool }}
{% endfor %}

{% for server in chrony_servers | default([]) %}
server {{ server }}
{% endfor %}
