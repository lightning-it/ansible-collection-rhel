---
argument_specs:
  main:
    short_description: Configure Firefox on RHEL using the shared lit.rhel.firefox role.
    options:
      firefox_config_enabled:
        type: bool
        default: true
