# Deprecated Vagrant Workflow

The Vagrant files in this directory remain only for backward compatibility with
older local workflows. New local deployment and system-level testing should use
the Incus workflow in `deploy/incus/`.
